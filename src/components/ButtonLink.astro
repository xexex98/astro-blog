---
interface Props {
  href: string;
  className?: string;
  ariaLabel?: string;
  title?: string;
  disabled?: boolean;
}

const { href, className = "", ariaLabel, title } = Astro.props;
---

<a {href} class={`group ${className}`} aria-label={ariaLabel} title={title}>
  <slot />
</a>
