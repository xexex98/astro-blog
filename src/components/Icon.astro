---
export interface Props {
  icon?: string;
  svg?: string;
}

const { icon = "", svg } = Astro.props as Props;
const { default: innerHTML } =
  icon && (await import(`/src/img/svg/${icon}.svg?raw`));
---

<Fragment set:html={icon ? innerHTML : svg} />
